1.这部分实际上 RDS服务或者任何一个高可用的服务，在对外提供一个访问接口时,都会考虑的一个问题，假设该系统是一个单Master的系统，也就是实际上只有一台服务器可以执行所有的操作（读写）。此时当master服务器宕机后 ，完成相应的处理程序后，使用人员如何在不修改自己写死的host:port访问接口，笔者已知的做法是采用vip进行实现。或者采用Nginx进行反向代理实现。这个使得分布式系统在对外表现上就像是一台固定的永不停歇，一直具备可用性的实现是有待商榷，这部分需要再查询相应的论文进行汇总思路